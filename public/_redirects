# IMPORTANTE: Garantir que arquivos estáticos do Next.js sejam servidos corretamente
# Os wildcards mais específicos devem vir primeiro
/_next/static/*  /_next/static/:splat  200!
/_next/data/*    /_next/data/:splat    200!
/_next/*         /_next/:splat         200!

# API routes
/api/*  /api/:splat  200!

# Rotas de autenticação
/auth/*  /auth/:splat  200!

# As rotas SPA vêm por último (para páginas não encontradas)
/*  /index.html  200 